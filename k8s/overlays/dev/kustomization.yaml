resources:
  - ../../base
  - namespace.yaml
  - local-storage.yaml

transformers:
  - |-
    apiVersion: builtin
    kind: NamespaceTransformer
    metadata:
      name: customTransformer
      namespace: dev
    unsetOnly: true
